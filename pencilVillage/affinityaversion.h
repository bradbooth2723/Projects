struct Affinity
{
    bool field;
    bool scrub;
    bool woods;
    bool rocky;

    Affinity(): field(0), scrub(0), woods(0), rocky(0) {}
};

struct Aversion
{
    bool field;
    bool scrub;
    bool woods;
    bool rocky;

    Aversion(): field(0), scrub(0), woods(0), rocky(0) {}
};
